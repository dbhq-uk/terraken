{
  "name": "terraken-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 100.115.72.85 --port 4332",
    "build": "astro build",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test",
    "preview": "astro preview --host 100.115.72.85 --port 4332"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.6"
  }
}
