dist/
/terraken
/tken

# Plan files are secrets - see README.md and SECURITY.md. A plan can hold
# credentials in the clear whether or not Terraform marked them sensitive,
# so the default names must never be committable by accident.
plan.json
*.tfplan
*.tfplan.json

# the site, which lives in site/ and is built by its own workflow
node_modules/
site/dist/
site/.astro/
